{{ if .Values.aws.visibility.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: otterize-dns
spec:
  selector:
    app: otterize-dns
  ports:
    - protocol: UDP
      port: 53
      targetPort: 53
{{ end }}
