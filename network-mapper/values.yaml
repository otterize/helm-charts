# Local configuration for network mapper chart
mapper:
  image:
    repository: "353146681200.dkr.ecr.us-east-1.amazonaws.com/otterize:mapper-latest"
    pullPolicy: Always

sniffer:
  image:
    repository: "353146681200.dkr.ecr.us-east-1.amazonaws.com/otterize:sniffer-latest"
    pullPolicy: Always

debug: false
allowGetAllResources: true

# Fields from All-in-one chart
global:
  # If defined overrides `allowGetAllResources`
  allowGetAllResources:
